<div class="container">
 <div class="row">
  <div class="mx-auto">

   <h4 class="mt-5 mb-4">商品編集</h4>
     <%= form_with model: @item,url:admin_item_path,method: :patch do |f| %>
     <table>
     <tr>
       <th><label for="inputImage">商品画像</label>
       <%= f.file_field :image, placeholder: "商品画像", accept: "image/*" %>
       </th>
     </tr>
     <tr>
         <th><label for="inputName">商品名</label>
         <%= f.text_field :name, autofocus: true, id:"inputName", placeholder:"商品名"%>
         </th>
     </tr>
     <tr>
         <th><label for="inputIntroduction">商品説明</label>
         <%= f.text_area :introduction, placeholder: "商品説明" %>
         </th>
     </tr>
     <tr>
         <th><label for="inputGenreName">ジャンル</label>
         <%= f.select :genre_id, options_from_collection_for_select(Genre.all, :id, :genre_name) %>
         </th>
     </tr>
     <tr>
         <th><label for="inputPrice">税抜価格</label>
         <%= f.number_field :price, placeholder: "税抜価格" %>円
         </th>
     </tr>
     <tr>
         <th><label for="inputPrice">販売ステータス</label>
          <%= f.radio_button :sales_status, true %>
          <%= f.label :sales_status,"販売中" %>
          <%= f.radio_button :sales_status, false %>
          <%= f.label :sales_status,"販売停止中" %></th>
     </tr>
     <tr>
         <th><%= f.submit "変更を保存",class: 'btn btn-success' %></th>
         <% end %>
     </tr>
     </table>
 </div>
</div>
</div>
