<div class="container">
 <div class="col-md-8 offset-md-1">

   <h4>注文履歴</h4>
   <table class="w-100 table">
      <tr>
       <th>注文日</th>
       <th>配送先</th>
       <th>注文商品</th>
       <th>支払金額</th>
       <th>注文詳細</th>
      </tr>

      <tr>
       <td><%= @order.created_at %></td>
       <td><%= @order.delivery_postal_code %>
           <%= @order.delivery_address %>
           <%= @order.delivery_name %>
       </td>
       <td><%= @order.order_items.items.name %></td>
       <td><%= @order.billing_amount %></td>
       <td><%= link_to "表示する", order_path.id,class: 'btn btn-primary' %></td>
      </tr>
      <% end %>
    </table>

 </div>
</div>